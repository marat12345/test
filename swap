#include <stdio.h>
void swap(int *x, int *y)
  {
  int temp = *x;
  *x=*y;
  *y=temp;
  }
int main(void)
{
  int x= 22;
  int y= 50;

  ptintf("x is %i\n", x);
  ptintf("y is %i\n", y);

  swap(&x, &y);

  ptintf("x is %i\n", x);
  ptintf("y is %i\n", y);

}
